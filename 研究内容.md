## 一、几何约束下的骨面增量重建（v2）

**层次定位：算法层。** 输入为 CT 重建三角网格骨面与球形磨钻位姿轨迹，研究动态骨表面的表示、增量更新和质量保持，输出当前骨面及其几何误差与质量信息，为实时显示和计划状态分析提供几何基础。

针对肩胛盂连续磨削中反复布尔运算产生碎小三角形、局部重网格引入几何漂移的问题，研究球形磨钻扫掠约束下的骨表面局部重建方法。以初始骨面与累计工具扫掠共同定义材料去除后的几何边界，研究几何状态与三角网格离散表示的分离机制，减少上一时刻网格误差向后续磨削过程的传递。在满足单值投影条件的局部区域，利用解析扫掠边界重采样骨面；进一步研究局部参数化的适用性判据、重建区域与未修改骨面的边界相容条件，以及几何偏差与三角形形状质量的联合约束。

拟研究的创新点是建立**扫掠几何约束与网格质量约束协同的局部重建机制**，使网格生成直接受目标几何和质量条件控制，并分析局部坡度、采样间距与表面逼近误差之间的关系。算法设计须明确数学模型、适用假设与边界相容条件，为几何偏差和质量约束提供推导或已有理论依据，区分已证明性质、数值验证和待证命题。通过解析平面、球窝和真实肩胛盂局部曲面，对比具有明确算法来源的连续布尔、布尔后质量维护与几何约束重建，在可比输入与精度预算下检验累计误差、最小角、边界连续性和重复磨削漂移，并通过消融识别各机制的作用，明确方法成立与失效的条件。

依据27号核心路线决策，固定单高度图不再作为完整任务的唯一表示，仅保留为已验证适用域内的候选加速模块与参照。下一机制研究初始骨几何与三维裁剪扫掠的独立目标表达，以及活动曲面、交线和三角形质量协同的局部离散。已有三维符号场只完成工具集合成员验证，未接真实骨面；普通等值面提取和均匀加密已有质量反例，不能当作完整替代算法。31号在解析轴对称竖直磨削中验证曲面分层生成，发现将C1光滑连接强制成环会产生薄带差面；仅保护实际坑口折线的消融在两档尺度共18个候选中通过形状检查，双向抽样误差小于0.021 mm，但没有整体误差证书、真实骨面拼接或强基线竞争证据。这是边界类型区分的有限机制证据，不是一般三维重建完成。须继续验证裁剪产生的真实折线与质量约束可行性，再决定具体核心实现；三角surface输出与CUDA必做不变，不预先宣称新表示具有创新或一般保证。

已有解析高度图实验及单个真实CT肩胛骨的局部图域与磨削实验，为“依据初始几何与累计扫掠更新、避免继承布尔碎片连接”提供了受限场景的初步依据（见16号实验报告）。17号的12组固定边界候选未同时通过质量与几何门槛；19号进一步采用质量反馈扩域与误差反馈采样，通过固定边界及两项消融对照，取得合格初始整骨拼接，并在浅磨、中等深度两条局部轨迹上各完成16步逐状态验收。20号使用历史扫掠足迹上的保守原面上界替代全域最高点，使原先拒绝的z=1.8 mm局部配置完成16步整骨验收，最大去除约1.198 mm；更低球心与固定边界负例仍拒绝。当前仅覆盖经验证的单值图域、水平等高扫掠及初始化后的固定底网，未覆盖原138步计划。一般局部参数化、随磨削动态扩域、一般收敛与质量保证仍待研究，旧逐步质量维护路线的真实骨面第17步失败未由本实验直接解决，不能将受限结果推广为一般曲面的质量保证或下游分析可用证明。

24号已接入TOG2025 Geogram作者库，在共同初始拼接骨面上完成两档工具离散的16步连续布尔诊断。修正了默认OBJ序列化损失并保留负例；双精度结果仍未满足逐面质量要求，但更密工具的共同采样最大误差优于当前局部方法。尚须补齐布尔后的竞争性质量维护、独立误差验证与公平驻留计时，不能宣称本方法全面优于强基线。

26号进一步在同一真实骨标本上验证七组轨迹，CPU/CUDA发布状态一致：旋转、z=1.5 mm及同物理路径改变分段等五组全程通过，平移1 mm在第7段因最小角拒绝，边界轨迹第1段因角度和误差拒绝。因此20号“更低球心拒绝”只代表当时配置，不是统一深度下限。恢复原138段裁剪计划后，当前固定图域在首段预检拒绝，尚未实际连续执行原计划。Geogram默认及禁共面简化配置在连续网格反馈中分别于第12/10段布尔超时；加入逐步变更面维护仍未形成有效连续合格磨削。此为适配链与配置的负结果，不足以判断原论文优劣；正式强维护基线、跨图域裁剪表示与动态质量机制仍未完成。

## 二、时空协同的骨面实时可视化（v2）

依据用户最新决定，正式性能研究采用CUDA计算路线，后续在远程或更换后的NVIDIA CUDA设备实现并验证；本机CPU用于正确性基线，已有OpenCL核显实验仅作探索证据，不能替代CUDA交付。CUDA加速仍须包含数据传输、同步、逐状态质量验收及显示在内的端到端实测，不能由后端选择直接推断实时达标。

23号已在用户提供的远程CUDA设备完成6组原生双精度扫掠查询对拍，并构建运行Geogram作者示例。当前仅证明受限算子可执行及基线入口可用：不含网格更新、误差证书、质量验收与显示，不代表完整CUDA交付或算法优于已有方法。

24号进一步将CUDA扫掠查询接入实际原面定位后的顶点更新和误差证书管线。CPU与CUDA各3轮16步均通过相同逐状态验收、发布状态一致；保留CPU镜像审计后CUDA约1446 ms/步，CPU约1192 ms/步，尚无净加速。主要费用包括CPU审计与原面/证书计算，CUDA内核平均约2.8 ms/步；需有数学依据的数值保障及完整时序/显示验证，不能由微基准收益推断实时性。

25号在受限扫掠公式上构造向外舍入区间，将数值宽度补入证书，正式轮不再逐查询CPU镜像；另设全查询CPU对拍和100位高精度包含测试。最终三组各3轮16步全部验收通过，CUDA区间512面批次平均584.72 ms，同期同批次CPU1167.29 ms，约2倍，但全部步骤仍超过100 ms。该证据支持减少镜像审计的受限可行性，不是整个几何系统的形式化保证。整批历史缓存因命中率低未取得净收益，不能概括为驻留必然有效；完整显示、一般动态拓扑、强竞争维护对照及原138步仍未完成。

**层次定位：系统层。** 承接工作一的骨面更新方法，结合手术计划和工具位姿序列，研究从工具运动到三维视觉反馈的实时计算与显示，输出连续显示的骨面和工具状态，以及状态时间戳、端到端延迟和分项性能数据。

28号将已有研究结果接入Qt/PyVista网格—指标联动工作台，并为原在线入口加入同状态曲线。29号补充播放视角保持回归，并在恢复的CUDA实例上完成RXMesh作者Remesh两组真实骨面单次运行（显式GLM头文件编译适配，算法源码不变）；内部重网格计时约905/928 ms，尚无输出网格质量与误差审计，不是同精度正式竞争结果。30号将既有受限局部重建与整骨验收接入CPU在线窗口，默认启动入口不再直接显示旧布尔结果；16步逐状态与既有实验顶点一致，拒绝停止发布，网格与刀位同版本。该成果是受限算法的在线可视化验证载体与GPU动态维护基线初跑，不是新三维核心实时闭环，不能记为CUDA拓扑数据组织已接入磨削。完整裁剪、跨域、竖直磨削下的质量生成仍按27号关口推进。

针对磨削作用局部集中、几何更新耗时波动，以及工具位姿、骨面和计划状态可能更新不同步的问题，研究局部计算与显示时序协同的方法。空间上，利用工具扫掠范围筛选受影响区域，组织局部相交检测、曲面更新、质量维护、法向及状态字段计算，减少未变化区域的重复处理；时间上，研究几何计算、状态计算与渲染的调度关系，使显示骨面、状态字段及其对应工具位姿保持一致，并显式表达相对于最新输入的滞后。分析几何精度、局部计算规模与反馈延迟之间的关系，确定不同负载下的适用条件。

拟研究的创新点是建立**面向局部连续磨削的增量计算与一致性显示协同机制**，在给定精度要求下协调更新范围、更新频率和显示时机，减少重复计算和不同时间状态混用造成的视觉偏差。以 CPU 管线和现成渲染组件建立基线，将 GPU 关键计算环节实现及验证列为必做研究任务，依据实测瓶颈确定加速对象，并从方法设计阶段分析并行性、数据依赖和传输开销；不能仅以GPU渲染代替计算研究。自适应分辨率与时间步长作为候选手段，不预设必须实现联合自适应算法。若采用尺寸场调节，工作一负责重建的几何正确性和质量约束，本项负责计算预算与更新时间的协调；AMBER 仅作为可选尺寸预测方法，不替代几何主干。

采用全量与局部计算、串行更新显示与协同调度的对照，并必须开展同精度 CPU/GPU 对比与数值一致性检验，分析局部化、调度和并行计算各自的贡献。检验端到端延迟、状态滞后、超时比例、内存及长序列稳定性，并报告均值、分位数和极值。已有局部筛选、分项计时和交互原型提供了实验基础；20号完成核显双精度扫掠微基准，21号进一步接入实际原面查询后的顶点更新与误差证书，三轮16步CPU/GPU状态一致且验收通过。由于保留逐查询CPU数值审计，整体尚无加速；尚需设备舍入误差预算、完整显示与协同机制验证。实时性须在包含传输、同步、质量检查、状态计算和显示的完整过程上验证，不能以GPU使用、渲染帧率或未加入质量维护的旧计时替代实测结果。

## 三、不确定性感知的磨削状态表达（v2）

**层次定位：应用与验证层。** 基于前两项提供的当前骨面、误差信息和显示时序，比较当前面与计划面，研究剩余量、完成度、过磨及安全边界的状态表达，并在完整系统上开展联合评价。

针对磨削过程中局部剩余量、整体完成度与过磨风险难以统一理解，且确定性偏差显示不足以表达结果可信度的问题，研究面向计划执行的骨面状态度量与视觉表达方法。以计划坐标系中的尚需去除区域、剩余磨削深度、计划内去除量和工具到安全边界的距离为基础，明确完成度、过磨与越界的判定口径，研究深度色阶、计划面叠加和局部风险提示的协同表达。在此基础上，结合几何误差、配准偏差、工具标定与跟踪误差及显示延迟，选择具有明确数据或假设依据的误差来源，研究其向局部状态判定的传播关系，将单一偏差值扩展为带有可信范围的状态描述。

拟研究的创新点是建立**融合几何可信度与时间有效性的磨削状态判定及视觉表达方法**，将前两项研究给出的表面误差和更新滞后纳入风险判断。对无法给出概率依据的误差采用保守范围表达，避免将误差界解释为统计置信区间；对具备概率模型的扰动，研究局部超限概率的估计与校准。通过可控误差注入和具有参考真值的磨削序列，对比确定性色阶、固定安全带与不确定性感知表达，评价过磨漏报、误报、风险区域定位偏差及状态提示延迟，并报告尾部事件。

综合评价贯穿三项研究，并在最终系统中联合验证几何误差、计算效率、网格质量、连续运行稳定性及可视化效果：简单场景采用解析真值，复杂场景与独立离线高精度结果或已有表面处理方法对比。已有原型提供了当前骨面、计划区域、剩余量、完成度和过磨显示，为状态表达与对照实验提供了载体；多源误差传播、可信范围计算及其视觉表达尚未完成验证。研究以非临床仿真和客观评价为主，条件允许时补充风险识别正确率与判断时间实验。界面实现作为方法验证工具，研究结论限定于所用数据与误差假设，不据此作临床安全性承诺。

## 附：与原始课题内容的对应关系

依据用户摘录的《硕士课题方向介绍_最终版.docx》中“课题拟研究的主要内容”和“可重点探索的研究方向”，上述三个研究内容保留“算法层—系统层—应用与验证层”的组织方式，将原有五项任务归入相互衔接的三层研究，并在各层细化方法问题。

### 三层研究链与分工

```text
输入：CT 重建三角骨面 + 手术计划（目标骨面/植入物 CAD）+ 球形磨钻位姿轨迹
    ↓
工作一：几何约束下的骨面增量重建（算法层，v2）
    扫掠几何 → 增量更新与局部重建 → 质量保持
    输出：当前骨面 + 几何误差与质量信息
    ↓
工作二：时空协同的骨面实时可视化（系统层，v2）
    局部筛选 → 调用更新方法 → 法向与状态字段计算 → 一致性显示
    输出：连续视觉反馈 + 状态时间戳与滞后信息 + 性能数据
    ↓
工作三：不确定性感知的磨削状态表达（应用与验证层，v2）
    当前面与计划面比较 → 剩余量/完成度/过磨/边界/可信范围 → 联合评价
    输出：状态与风险表达方法 + 几何、性能、质量、稳定性及可视化评价
```

上述顺序表示依赖关系：工作一研究骨面如何正确更新，工作二研究如何及时、一致地显示更新结果，工作三定义所显示状态的含义并验证其表达效果。工作三的状态度量方法接入工作二的显示过程，各项局部验证随方法开发开展，最终再进行系统联合评价。

### 主要对应关系

| 新版研究内容 | 对应“课题拟研究的主要内容” | 对应“可重点探索的研究方向” |
| --- | --- | --- |
| 一、几何约束下的骨面增量重建（v2） | 以第2项“动态骨表面表示与更新”为主，包含第3项中的局部曲面更新 | 主要对应“动态表面的局部重建与质量保持”，结合“骨表面的增量更新与实时实现”中的更新方法 |
| 二、时空协同的骨面实时可视化（v2） | 以第3项“实时计算与显示”为主，承接第2项的更新方法 | 主要对应“骨表面的增量更新与实时实现”中的数据组织与实时显示；“多分辨率和自适应 surface 表达”作为候选手段 |
| 三、不确定性感知的磨削状态表达（v2） | 以第4项“计划执行状态与风险表达”为主，承担第5项的系统联合评价，由第1项的显示需求提供依据 | 直接对应“计划误差和不确定性可视化” |

两项基础工作融入三个研究内容：

- **第1项“手术场景与可视化需求分析”**：作为三项研究共同的应用依据，用来确定输入、约束和需要表达的信息，没有单列为方法创新。
- **第5项“方法评价”**：分别融入三项研究的验证环节，包括几何精度与质量、效率与延迟、风险表达准确性等。

### 相较原始课题进一步凝练的部分

1. **第一项**把“局部重建与质量保持”具体化为：由初始骨面和累计磨钻扫掠约束重建，研究如何减少连续更新的误差传递。
2. **第二项**把“实时计算与显示”具体化为：研究局部增量计算、状态同步与显示调度如何协同，在给定精度要求下控制视觉反馈延迟。相较此前“时空自适应”版本，恢复独立系统层定位，不再将空间密度与时间步长联合自适应列为预定核心方法；GPU计算研究为必做，具体加速环节根据性能瓶颈选择，自适应仍为候选手段。
3. **第三项**保留完整的计划执行状态与风险表达，在此基础上研究误差如何影响剩余量、过磨和越界判断，再表达这些判断的可信范围，并开展系统联合评价。

### 研究重点与范围控制

原始课题建议从可探索方向中“选择一个作为主要研究重点”。三层结构用于保持课题完整性，不要求三项均发展为同等强度的独立算法创新。结合当前实验基础，采用以下主次定位：

**以第一项增量重建与质量保持为主攻，第二项研究其计算与显示的协同，第三项承接计划状态与风险表达及应用验证。**

这一定位用于控制研究投入和论文贡献层次；三个研究内容仍相互衔接，第二、三项的方法设想仍需通过实验验证，不能因列入范本而视为已完成创新。各项算法设计均须有数学依据，并以已有算法对照与消融支撑贡献；工程原型是验证工具，不替代理论分析与学术证据。

正式竞争基线论文须满足CCF-A、CCF-B或明确年度与分区口径的SCI一区之一，并核验作者实现与论文对应关系。近期方法与仍有竞争力的经典方法共同构成对照，按几何更新、质量维护与CUDA动态处理分别匹配任务；不得仅以流程搭建或弱基线对照宣称算法先进性。
