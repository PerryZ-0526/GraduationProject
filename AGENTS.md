# GraduationProject 项目协作说明

> **修改时间及内容补充**：2026-09-09 12:19:56（北京时间），新增31号原计划分类与竖直磨削分层机制报告。138段分类为134水平/4竖直，92段需径向裁剪，不是执行完成。新增`初步实验/竖直磨削曲面分层/`，用解析开放面验证球底—圆柱C1连接与坑口折线区分；两档尺度、9状态、两种机制共36候选，强制全部连接环有4个形状失败，去除非特征光滑环的18候选形状均通过，仍无整体距离证书/真实骨面拼接，accepted保持false。5项新增、旧三维5项/联合6项及真实16步快照回归通过。远端SSH连接失败，未执行新强基线或CUDA，不作算法优势结论；本机沿用`.venv`，无新依赖/环境配置，在线16步入口未改。同步研究范本与18号；下一研究编号32，详细参数、数学依据、反例和复现命令见31号。

> **修改时间及内容补充**：2026-09-09 10:51:30（北京时间），新增30号受限逐步重建在线接入报告。原`real_bone_interactive_app.py`命令默认进入`validated_online.py`受限CPU在线模式，旧布尔须显式加`--legacy-bool`且标题标明不保证逐步质量；`--self-test`仍为旧布尔自检。`dynamic.py`批量/在线共享逐状态生成器，初态及每步通过原质量、误差与整骨复核后才交付独立快照；拒绝停止，GUI刀位与已发布网格同版本。新增5项、既有联合重建6项及相机2项通过，16步逐张与20号既有顶点一致；角≥25.926°、q≥0.5977、证书≤0.09471mm。一次窗口序列CPU计算验收均值810.794ms，100%超100ms，不含渲染等端到端成本。无新依赖/环境配置、无远端运行；CPU窗口不等于CUDA在线接入。同步研究范本和18号；原138段、一般动态重三角化与下游验收仍未完成。环境沿用`.venv`，入口、测试、导出及限制见30号。下一研究编号31。

> **修改时间及内容补充**：2026-09-08 15:11:00（北京时间），新增29号播放视角修复与远端续跑记录。原在线窗口及研究回放刷新显式保持完整相机，2项真实Qt相机回归、3项映射测试和GUI冒烟通过；旧窗口需重启。租用实例恢复，在`/root/autodl-tmp/graduation_project/build_rxmesh_dynamic_20260908_141858/cmake_venv`安装cmake==3.31.6，不改系统及主venv；OpenMesh8.1使用官方归档、GLM使用作者固定提交归档，Remesh构建显式设置`NVCC_PREPEND_FLAGS=--pre-include=glm/gtx/norm.hpp`以补无窗口编译声明，不改作者算法源码。RTX4080SUPER/CUDA12.8.93上两组真实整骨作者Remesh各1次运行完成，内部计时904.540/927.811ms；尚无输出OBJ质量/误差审计，float32/fast-math不是项目FP64公平竞争结论。同步更新研究范本及18号；GPU动态拓扑尚未接入磨削，原138段仍未完成。入口、日志及依赖摘要见29号；本机无新依赖。下一研究编号30。

> **修改时间及内容补充**：2026-09-08 14:27:30（北京时间），新增28号界面与强对照接入记录。原Qt/PyVista新增研究结果工作台及在线指标面板，3项映射测试、结果GUI与旧入口实际三步验证通过，非新算法质量/实时验收。RXMesh作者Remesh快照e468c34ffabc70cd207309bce662d4821a9ed3b7隔离配置因CMake3.22.1失败；准备固定cmake3.31.6时SSH中断，隔离venv及安装未执行，GPU动态重网格未运行。待用户确认实例/端点，不反复重试；结果见`初步实验/RXMesh动态对照/result.json`。无本机新增依赖，无系统/主venv变更。下一研究编号29；完整裁剪/跨域/竖直合格重三角化及实时闭环仍未完成。

> **修改时间及内容补充**：2026-09-08 13:58:48（北京时间），依据用户要求与27号机制实验，固定单高度图退出完整任务唯一核心表示，旧实现保留作受限对照；暂停其外围优化。新增三维裁剪扫掠符号参照，5项测试及138段累计工具成员抽样一致，不是实际骨面138步。普通等值面提取8张解析网格7张质量失败，不能用隐式场加密替代质量生成。同步研究范本及18号，下一编号28。新增入口`初步实验/三维目标几何机制/test_target.py`、`experiment.py`，运行用本机既有`.venv`，无新增依赖/Conda/Docker，未启用远端；完整环境、负例、源码摘要见27号。后续必须先验证目标边界与交线约束生成机制，不能把符号场称为距离证书或新算法创新。

> **修改时间及内容补充**：2026-09-08 13:32:05（北京时间），新增26号真实骨面完整计划覆盖与强基线维护报告。七组CPU/CUDA配对共188次接受、4次拒绝、48段未执行；原138段在首段适用域预检拒绝，不能算作执行完成。Geogram默认/禁共面简化诊断在第12/10段布尔超时，逐步维护未取得有效连续合格磨削；不得据此宣称优于作者算法。同步研究范本及18号，下一研究文档编号27。本轮沿用远程环境，无新增依赖；入口、源代码快照、逐步数据和环境命令见26号。GPU必做不变，当前主阻碍包含表示覆盖和质量机制，不是仅硬件性能。

> **修改时间及内容补充**：2026-09-08 13:00:53（北京时间），新增25号CUDA区间过滤与查询批量化报告。区间512面批次584.72 ms/步，同期CPU1167.29 ms/步；144次正式更新与16次独立全查询对拍通过，仍100%超过100 ms。整批历史缓存无净收益，保留负例。41项不同检查通过，无新增环境依赖。同步研究范本及18号，下一研究编号26；不得将受限扫掠区间论证扩展成整系统形式化证明或新算法全面优越。

> **修改时间及内容补充**：2026-09-08 12:29:00（北京时间），新增24号真实骨面Geogram对照与CUDA逐状态接入报告。CUDA和CPU各3轮16步共96次验收通过，状态一致；CUDA含CPU审计平均1446 ms/步，CPU1192 ms/步，未加速。Geogram双精度适配后完成两档各16步诊断，仍有质量不合格且s3采样最大误差优于本方法，不宣称全面优势。35项远端检查通过，保留有损OBJ首轮失败证据，下一编号25。同步更新研究范本与18号。

> **修改时间及内容补充**：2026-09-08 11:59:00（北京时间），用户提供租用CUDA设备并授权配置实验。新增23号报告和独立CUDA远程验证入口；原生CUDA双精度扫掠6组通过，Geogram无窗口构建和作者示例运行成功，均不代表完整磨削竞争实验完成。同步更新研究范本与18号状态。凭据仅在被忽略且限制ACL的`.env`，不得输出或上传。下一研究文档编号24。

> **修改时间及内容补充**：2026-09-08 10:56:00（北京时间），按用户要求将正式竞争论文门槛明确为CCF-A、CCF-B或SCI一区，并将CUDA列为后续正式计算路线。新增22号高水平基线拉取报告；四个作者仓库及其Git子模块独立拉取，三份有效论文PDF已核验，RXMesh全文下载受阻，不能记为完成。同步更新研究范本和18号规划。未安装CUDA、Conda或Docker环境，下一研究文档编号23。

> **修改时间及内容补充**：2026-09-08 10:40:44（北京时间），依用户要求强化算法先进性与近期强基线约束：工作流程不能替代算法贡献，不能仅比较2022年或自身旧版本；先核验近期高水平论文及对应代码，再冻结竞争实验。本次仅检索核验，无新增算法或环境安装。

> **修改时间及内容补充**：2026-09-08 09:04:38（北京时间），新增21号核显证书管线与连续布尔参照报告。实际GPU参与顶点/证书查询，但保留CPU逐查询审计，整体未加速；Manifold双精度初步参照显示精度与质量取舍。同步更新`研究内容.md`及18号状态。31项测试通过，无新增依赖；下一研究文档编号22。

> **修改时间及内容补充**：2026-09-08 08:51:14（北京时间），新增20号局部适用域与核显双精度扫掠报告；局部上界使z=1.8 mm配置完成16步整骨验收，仍拒绝更低球心与边界扫掠。同步更新`研究内容.md`第一、二项及18号规划，依据为20号实测结果。新增PyOpenCL环境，实际核显计算已验证但完整实时管线未完成；下一编号21。

> **生成时间**：2026-09-02 17:13:13（北京时间）
> **修改时间及内容**：
> - 2026-09-02 17:13:13 首次生成：依据初始课题文档、现有 01–17 号调研文档及 `reference/` 参考代码，固化项目目标、资料优先级、实施路线、研究边界、代码与文档规范、验证要求。
> - 2026-09-02 17:21:06 目录重整：将根目录 13–17 号文档移入 `课题规划与专题调研/` 并重新编号为 01–05，更新资料路径、执行基线编号和后续文档编号规则。
> - 2026-09-02 17:25:45 新增 `课题规划与专题调研/06-课题调研进展汇报.md`，并将该目录下一建议编号顺延为 07。
> - 2026-09-02 17:48:46 新增 `课题规划与专题调研/07-AMBER与本课题嵌合方案.md`，并将其内容作为小节纳入 06 号进展汇报，下一建议编号顺延为 08。
> - 2026-09-04 19:34:11 新增 `课题规划与专题调研/08-真实骨面驻留式系统演示与性能验证.md`，记录真实 CT 肩胛骨上的系统演示与性能验证，下一建议编号顺延为 09。
> - 2026-09-07 00:47:35 新增 `课题规划与专题调研/09-真实骨面交互式原型架构与使用说明.md` 与真实骨面交互式桌面原型；当前正式运行环境为项目根目录 `.venv`，窗口层采用 PyVistaQt、VTK 与 PyQt6，下一建议文档编号顺延为 10。
>
> **文档概述**：本文件面向在本目录及其子目录工作的 Codex/智能体，规定如何理解和推进“面向机器人辅助肩关节置换的骨表面实时可视化研究”。当前以学术研究问题、数学依据和可复现实验证据组织工作；CPU用于正确性与性能基线，GPU计算实现与同精度对照为必做研究环节，神经网络和增强现实仍为可选增量。`AGENTS.md` 是工具约定文件，文件名不参与普通研究文档的数字编号。

> **修改时间及内容补充**：2026-09-07 01:43:00，新增10号网格质量质疑与交付优化报告，增加局部重网格分析副本及验收，环境增加PyMeshLab；下一文档编号为11。

> **修改时间及内容补充**：2026-09-07 16:53:00，新增11号逐步质量维护实验与未达标问题报告；动态实验采用双精度转换、每步维护回灌及拒绝回滚。当前固定参数仅发布16/138步，第17步拒绝，尚未满足全程稳定性与100 ms实时目标；下一研究文档编号为12。

> **修改时间及内容补充**：2026-09-07 17:13:53，新增12号解析高度图局部重建第一阶段报告与隔离实验。27组试验中17/18常规场景全序列通过，9组负例拒绝；64步重复轨迹无新增顶点漂移。固定底网高度更新不等于任意真实骨面重三角化，未解决真实骨面第17步问题，未宣称实时达标；下一研究文档编号为13。

> **修改时间及内容补充**：2026-09-07 17:26:04（北京时间），根据用户要求生成根目录 `研究内容.md`，将三项研究凝练为几何约束下的骨面局部重建、误差驱动的骨面时空自适应、不确定性感知的磨削状态可视化；确立其为后续研究内容的基底范本，补充资料优先级、持续维护规则和该文件的格式例外。该文档生成时间为2026-09-07 17:26:04（北京时间）。

> **修改时间及内容补充**：2026-09-07 17:32:52（北京时间），依用户要求在 `研究内容.md` 追加与原始课题五项主要内容、四个探索方向的对应说明，补充进一步凝练之处及“第一项主攻、第二项服务精度与实时性、第三项承接状态表达与验证”的建议定位；依据为用户提供的原始Word文档摘录和本次问答。同步调整格式例外，允许保留该对应说明附录。

> **修改时间及内容补充**：2026-09-07 17:36:49（北京时间），将根目录原18、19、20号文档归档至 `课题规划与专题调研/`，依次重编号为13-最小工作流验证报告.md、14-初步实验设计与管线规划.md、15-E1真实骨面场景实验记录.md；同步修正关联编号与脚本中的文档引用，下一研究文档编号为16。

> **修改时间及内容补充**：2026-09-07 17:42:25，新增16号真实肩胛盂图域与局部磨削报告，保留同期归档的13—15号文档。中心12×12 mm图域通过覆盖、重叠、遮挡和内部边界检查；7组局部配置中5组完成16步、2组拒绝。较深通过组最大顶点去除1.197 mm、最小角36.754°、误差上界0.07385 mm，平均更新约626 ms。同步更新`研究内容.md`第一项的已有基础与未完成范围，依据为16号报告；未拼接整骨、未执行原138步计划、未达实时目标。下一研究文档编号为17。

> **修改时间及内容补充**：2026-09-07 17:51:16（北京时间），依据用户确认的三层研究结构，更新 `研究内容.md` 为“几何约束下的骨面增量重建—时空协同的骨面实时可视化—不确定性感知的磨削状态表达”。恢复算法层、系统层、应用与验证层的分工，保留最新16号实验基础与适用边界，同步更新原始课题对应附录及研究链；联合自适应和GPU均为依据瓶颈选择的候选手段，不是预定必达创新。
>
> **修改时间及内容补充**：2026-09-07 18:02:00（北京时间），按用户要求为旧版三项研究工作统一标记为 v1，为根目录 `研究内容.md` 当前三项研究统一标记为 v2；历史文档正文和实验结论未改写。

> **修改时间及内容补充**：2026-09-07 18:04:14（北京时间），新增17号真实骨面共边拼接与过渡带质量报告及独立实验目录。12组初始候选均水密但无一通过全部门槛，未启动磨削或接入GUI；同步更新`研究内容.md`第一项已有基础，保留同期v1/v2标记。新增triangle==20250106，使用同一`.venv`，商业用途须核对底层Triangle许可证。下一研究文档编号为18。

> **修改时间及内容补充**：2026-09-07 18:12:29（北京时间），依用户明确决定，将GPU计算实现及CPU/GPU对照列为必做，确立数学依据、已有算法对照和消融为学术研究强制要求；同步修订`研究内容.md`及新增18号v2实验规划。旧记录中的GPU可选、CPU最终保底不再作为当前执行规则，下一研究文档编号为19。

> **修改时间及内容补充**：2026-09-07 18:43:46（北京时间），优先收尾边界与过渡带问题，新增19号联合重建、四组基线/消融及整骨逐步验收报告。浅磨与中等深度各完成16步，逐面角≥25°、q≥0.4、误差证书≤0.1 mm；深切仍拒绝。同步更新研究范本及18号进展。只读核验本机Intel Arc 130T核显及OpenCL 3.0、cl_khr_fp64声明，不代表GPU计算实测；无新增环境依赖。下一研究文档编号为20。

## 索引目录

- [一、适用范围与总原则](#一适用范围与总原则)
- [二、资料权威顺序](#二资料权威顺序)
- [三、项目目标与研究边界](#三项目目标与研究边界)
- [四、当前三项研究工作](#四当前三项研究工作)
- [五、技术路线与参考代码](#五技术路线与参考代码)
- [六、实现与修改规范](#六实现与修改规范)
- [七、验证与实验规范](#七验证与实验规范)
- [八、文档与研究诚信规范](#八文档与研究诚信规范)
- [九、开始任务前的检查清单](#九开始任务前的检查清单)
- [十、当前运行环境](#十当前运行环境)

---

## 一、适用范围与总原则

1. 本文件适用于 `C:\Users\24848\Desktop\GraduationProject` 及其所有子目录；若未来某个参考仓库出现更深层的 `AGENTS.md`，以更深层文件对该子目录的规则为准。
2. 当前定位是学术研究，不是以工程演示或实验数量替代学术贡献；在毕业与资源约束下，优先形成明确研究问题、数学依据、方法贡献和可复现实验证据。
3. 可运行闭环用于验证方法，不等同于研究完成；复用成熟组件不等于自研创新，不要求凭空创造全新理论，但必须解释方法成立的依据与相对已有方法的区别。
4. 所有结论必须区分“文献已经证明”“当前调研推断”“待实验验证”。不得把目标值、推测或厂商宣传写成已完成结果。
5. 修改必须小而明确。只触碰完成当前任务所需的文件和代码，不顺手重构、不清理无关代码、不改动无关注释或格式。

## 二、资料权威顺序

出现冲突时，按以下顺序理解项目：

**研究内容专项优先规则**：根目录 [`研究内容.md`](./研究内容.md) 是用户指定的研究内容基底范本；[`课题规划与专题调研/18-v2研究实验规划与学术验证规范.md`](./课题规划与专题调研/18-v2研究实验规划与学术验证规范.md) 是当前实验执行规划。两者优先于01、04、06、07、14号中的旧版划分及工程化取舍，最新用户、导师决定仍具有上位约束。GPU已经由用户明确列为必做，不得再自动降为加分项；AMBER、神经网络和临床研究仍非必做。拟研究方法不代表已经完成。

1. **初始课题依据**：`硕士课题方向介绍_最终版.docx`。这是课题目标、范围和预期成果的原始依据；默认只读，未经用户明确要求不得改写或覆盖。
2. **当前执行基线**：18号v2实验规划。04号保留为历史资源与风险参考，其CPU最终保底、GPU加分及工作量导向策略已被最新学术验证要求取代，不能用于取消必做研究环节。
3. **理想研究上限**：`课题规划与专题调研/01-课题三大研究工作梳理.md`。用于理解完整研究链和候选创新，不得自动把其中所有加分项升级为必做项。
4. **定向文献与源码依据**：`课题规划与专题调研/02-三大研究工作文献调研报告.md`、`课题规划与专题调研/03-RXMesh动态网格核心代码分析.md`。
5. **神经网络边界**：`课题规划与专题调研/05-神经网络相邻选项调研.md`。神经网络仅可作为低风险外挂，不替代几何主干。
6. **逐篇证据**：`文献精读/01-参考文献逐篇分析.md` 与 `文献精读/02–12`。引用具体数字、方法或局限时，应回到对应精读文档和原始来源核对。
7. **外部参考实现**：`reference/`。这些目录主要是第三方或官方开源仓库快照，是实验底座和基线，不代表本项目自研代码。

若用户、导师的新决定与现有文档冲突，以最新明确决定为准，并同步更新相关规划文档，记录北京时间、修改内容和影响范围。

## 三、项目目标与研究边界

### 3.1 项目目标

面向机器人辅助反式肩关节置换中的肩胛盂磨削，输入 CT 重建三角骨面、术前计划/目标面和球形磨钻位姿轨迹，持续更新当前骨表面，并实时表达：

- 当前骨面和计划骨面；
- 尚需去除的区域与剩余磨削深度；
- 计划完成度；
- 过磨、越界及安全边界风险；
- 配准、跟踪和系统延迟等不确定性对显示结果的影响。

最终产出应形成“更新算法—可视化原型—状态/风险表达与评价”的完整数据闭环。

### 3.2 必须遵守的范围

- 主要对象：肩胛盂局部区域、刚性骨表面、球形磨钻。
- 主要表示：三角 surface/mesh；允许在保底或对照实验中使用体素、高度场、距离场等辅助表示。
- 主要任务：几何更新、实时计算、显示、误差与风险评价。
- 不作为毕业必要条件：机器人控制、软组织形变、完整手术仿真、临床系统认证、任意工具和任意复杂网格的通用布尔处理。
- 不开展真实临床试验或作临床有效性承诺；现阶段评价应定位为仿真、公开数据、合成数据、台架或小规模非临床用户研究。

## 四、当前三项研究工作（v2；下列旧版工程条目保留 v1 语境）

三项研究的正式学术表述及具体内容统一见 [`研究内容.md`](./研究内容.md)，保持“算法层—系统层—应用与验证层”。下列条目用于对应层次的实现基础，不替代数学论证、已有算法对照与消融。GPU计算研究为必做，具体加速环节根据实测瓶颈选择；自适应分辨率与时间步长仍为候选手段。避免将工具集成、界面开发或实验数量本身等同于方法创新。

### 4.1 工作一：磨削表面增量更新

- **MVP**：基于现成精确/鲁棒布尔库和局部重网格完成 CPU 更新管线；磨钻使用球体或球沿轨迹形成的胶囊/扫掠体近似。
- **必须验证**：平面、球窝等有解析真值的简单情形；复杂轨迹使用离线高精度方法对拍。
- **主要输出**：更新序列、几何误差、网格质量变化、失败案例与误差来源。
- **方法要求**：解析约束、局部重建与质量保持须给出数学依据、适用条件及对照证据；RXMesh空腔移植只是GPU技术路线候选，不指定为唯一实现。
- **CPU角色**：作为正确性参照和可复现实验基线；单项GPU路线失败不否定已有算法证据，但不能据此宣称GPU必做任务已完成。

### 4.2 工作二：实时可视化原型

- **MVP**：桌面原型可读取骨面、计划面和工具轨迹，调用工作一更新并显示当前面、计划面、剩余量和工具位置。
- 优先复用 VTK、OpenGL、PyVista 或现有参考库，不自研通用渲染引擎。
- 性能章节必须给出端到端延迟及各模块耗时，不能只报告渲染帧率。
- **必做项**：以CUDA实现与主要计算瓶颈相关的GPU计算环节，接入完整管线并进行同精度CPU/CUDA对比；只调用GPU渲染不算完成。CUDA为用户指定的正式路线，是否复用RXMesh及具体加速对象由证据决定。当前OpenCL核显实验仅为探索与正确性参考，不替代CUDA交付；后续在用户提供的远程或本地NVIDIA CUDA设备验证，不自行租用资源。
- **验收边界**：GPU不保证自动达到实时要求；必须报告传输、同步、质量检查、状态计算和显示在内的延迟及超时。CPU演示保留为基线，不能替代GPU研究交付。

### 4.3 工作三：状态/风险表达与综合评价

- 至少实现 2–3 类显示：剩余磨削量、完成度、过磨/越界警告；优先再加入配准误差带或安全边界。
- 客观评价至少覆盖：几何精度、效率、网格质量、连续运行稳定性。
- 风险评价同时报告均值、分位数、极值和超限/尾部事件，不得只给平均误差。
- 用户研究仅在条件允许时开展，可采用 NASA-TLX、李克特量表、任务时间和判断正确率；招募不足时退回专家访谈或以客观评价为主。

## 五、技术路线与参考代码

### 5.1 推荐顺序

1. 先跑通一个 CPU 布尔基线和解析真值测试。
2. 再加入局部重网格、法向更新和剩余深度/距离场。
3. 然后完成可视化原型和指标采集。
4. 从方法设计阶段分析并行性、数据依赖、局部更新规模和内存布局；在最小可信CPU参照形成后开展GPU计算实现与同精度验证，不必等待原138步全部完成才开始GPU研究。具体移植RXMesh或其他方案须以瓶颈为依据。

### 5.2 `reference/` 目录角色

- `RXMesh/`：官方 GPU 动态网格底座；重点关注 cavity manager、patch scheduler、ribbon/LPHashTable 和跨 patch migrate。
- `InteractiveAndRobustMeshBooleans/`：最接近增量布尔的官方参照实现，适合作为工作一基线。
- `OpenMeshCraft/`：精确交点与退化/近重合求交参照。
- `hypercut/`：第三方 BSP 实现，只实现原论文部分能力；128 位算术与约 0.22 mm 分辨率限制必须在使用前重新核对，不能直接宣称满足 0.1 mm 目标。
- `meshtaichi/`：CPU/GPU 网格编程与数据布局参考，不是当前主线的强制依赖。

这些仓库没有统一的根构建入口。进入具体仓库后读取其 README、CMake 配置和许可证，只构建当前任务需要的目标。不得默认一次性编译整个 `reference/`。

### 5.3 神经网络与相邻方向

- TotalSegmentator 可作为 CT 到骨面预处理的可选组件，优先使用预训练模型，不把它包装为核心贡献。
- TransMorph/VoxelMorph 仅可作为配准对照或误差来源分析的可选基线。
- NeuS、instant-ngp、GauSTAR 等神经表面方法只放在相关工作或展望，除非用户明确改变研究主线。
- 增强现实、HoloLens 和术中场景重建均为扩展项，不得挤占三项 MVP 的时间。

## 六、实现与修改规范

1. 新增代码注释必须使用中文。
2. 修改原来带注释的代码时，必须同步更新对应注释，使其准确描述新逻辑。
3. 未修改某段代码时，不得删除、改写或翻译其原有注释。
4. 涉及时间戳、日志、实验记录和文档元数据时，统一使用北京时间（Asia/Shanghai），格式优先为 `YYYY-MM-DD HH:mm:ss`。
5. 优先复用现有组件；不为单次需求创建抽象层、插件系统或配置框架。
6. 不对第三方参考仓库做无关格式化、批量重命名或全局重构。若确需修改参考代码：
   - 先记录上游仓库、版本/提交和本地状态；
   - 将改动限制在独立补丁或清晰的小提交范围内；
   - 区分“上游原始代码”和“本课题改造”；
   - 遵守对应许可证与仓库自身规范。
7. 不覆盖原始输入、原始 CT/网格、术前计划、参考结果或初始 DOCX。派生结果写入单独目录，并保留参数和来源。
8. 所有关键阈值、容差、单位和坐标系必须显式记录；距离默认使用 mm，角度默认使用 °，不要依赖隐式单位。
9. 若使用随机采样或蒙特卡洛实验，固定并记录随机种子、样本数、分布假设和置信区间方法。

## 七、验证与实验规范

### 7.1 修改前后验证

1. 修改前先运行与目标最近的最小示例或测试，记录基线。
2. 先构造能复现问题或验证目标的测试，再修改实现。
3. 修改后先跑局部测试，再按风险决定是否跑更大范围测试。
4. 报告实际运行的命令、硬件、数据规模、参数、耗时和结果；不能只写“测试通过”。

### 7.2 几何正确性

- 简单情形优先使用解析真值；复杂情形使用至少一种独立的高精度基线。
- 误差至少包含平均值、P95/P99、最大值和空间分布；必要时报告 Hausdorff/双向表面距离。
- 区分表面表示误差、布尔/重网格误差、配准误差、工具跟踪误差和显示延迟。
- `≤0.1 mm` 是当前表面更新的建议目标/误差预算，不是未经实验即可宣称的成果。

### 7.3 性能与稳定性

- 报告候选筛选、几何更新、重网格、法向更新、数据传输和渲染的分项耗时。
- GPU 实验记录 GPU 型号、CUDA 版本、编译选项、显存和预热策略；CPU/GPU 对比必须使用一致输入和精度设置。
- `60 fps`、`<100 ms` 是交互目标参考，不得用渲染帧率替代几何更新延迟。
- 长序列测试记录网格规模、退化三角形、内存占用、失败/重试次数及结果漂移。

### 7.4 退出与保底条件

- RXMesh 构建或核心移植连续3周仍无可测进展：保留CPU参照，评估替代GPU实现并报告路线问题；不得自行取消GPU必做要求。若需改变研究范围，须由用户或导师确认。
- GPU 加速比低于 2 倍：保留实验并解释空间集中负载等原因，不伪造或选择性隐去结果。
- 布尔/重网格长期退化：缩小研究范围、提高维护频率，或加入体素/高度场对照。
- 真实 CT 不可得：使用合成或公开肩胛骨模型与仿真轨迹，并明确数据局限。

### 7.5 学术研究强制规范（2026-09-07用户明确要求）

1. **先提出问题再实现算法**：每项方法改动先明确研究假设、现有方法局限、拟改进机制和可证伪的实验判据；不能以某个失败面编号驱动无原则修补。
2. **数学依据不可省略**：说明变量、单位、目标几何、约束、适用域和退化条件；关键设计给出推导、引用的已有理论或明确待证明命题。区分严格证明、含浮点容差的数值验证和经验观察；没有证明不得宣称一般质量保证、收敛性或误差上界。
3. **已有算法对比是必做**：基线必须有可核查的论文/算法来源及实现版本，不得仅与自己上一版代码比较。正式实验前核对原文、适用范围与实现对应关系；不得把第三方部分实现描述为完整复现。至少覆盖与主要贡献直接相关的已有方法，并保留有竞争力的基线，不能只选弱基线。
4. **公平对照与消融是必做**：共享输入、坐标、物理扫掠轨迹、精度预算和硬件计时口径，统一或如实披露调参预算；改变离散步长须保持物理轨迹可比。分别消融几何状态分离、边界/过渡带约束、局部化、调度和GPU等所声称的贡献，避免把多项改动共同收益归于单一机制。
5. **逐状态验收**：每次发布的重建区域（含过渡带）均检查预先规定的几何与质量门槛；记录未修改原网格的既有质量。拒绝、回滚、超时、未支持轨迹段均报告，不能计入成功磨削；只报告成功帧或末帧不合格。
6. **GPU属于研究交付而非装饰**：需要实际GPU计算、同精度CPU参照、数值一致性和端到端收益证据；报告传输/同步成本、规模变化、预热、重复次数、均值、P95/P99、最大值与超时率。不能通过降低精度、漏算扫掠或省略质量验收换取未经说明的加速。
7. **研究贡献与组件复用分开**：界面、环境、库调用和跑通示例是验证载体，不直接构成算法创新。数学与实验未支持的主张标记为待验证，负结果如实保留；实时性和临床安全性不得根据直觉或GPU使用与否推断。
8. **算法先进性与近期强基线（2026-09-08用户明确要求）**：研究目标是提出相对已有方法有可验证改进的算法，不是仅搭建工作流程。方法设计前检索近期高水平论文，核验正式发表年份、原文、作者代码、分支/版本、许可证和硬件条件；不得仅用2022年方法、自身旧版本或未维护布尔作为唯一对手。保留有竞争力的经典方法，不按年份自动淘汰；以任务匹配和竞争力选择基线。必须设置同精度、同质量目标下的强对照和消融，无法证实优势时调整假设或方法，不预设新方法必胜。2026-09-08核验候选包括2025 TOG Geogram精确CSG（https://doi.org/10.1145/3744642 ，https://github.com/BrunoLevy/geogram）、2024 TOG交点解析（https://mangoleaves.github.io/projects/mesh-arrangements/ ，https://github.com/mangoleaves/OpenMeshCraft ，论文原实现分支mesh_arrangements）、2025 TOG动态RXMesh（https://doi.org/10.1145/3731162 ，https://github.com/owensgroup/RXMesh）、2025 CGF/PG PaMO（https://seonghunn.github.io/pamo/ ，https://github.com/SarahWeiii/pamo）。它们分别是CSG、交点解析、GPU动态处理及重网格/简化，不可冒充同一种完整磨削算法；公开代码已核验不等于本机复现。RXMesh/PaMO的CUDA实现不能直接在本机Intel核显运行，Docker不消除硬件限制。2025无自交重网格候选https://ltrbless.github.io/projects/Remesh/ 的作者页面仍含占位链接，尚未核实可运行官方代码，不列为已可复现基线。

## 八、文档与研究诚信规范

**2026-09-08新增上位执行约束**：正式竞争基线论文必须满足CCF-A、CCF-B或SCI一区至少一项。CCF记录具体期刊/会议及官方目录来源；SCI须记录年度、学科以及JCR或中科院分区口径，不混用。辅助工程参照可保留，但不得替代合格强基线。22号记录了本轮版本和下载状态。上文OpenMeshCraft的`mesh_arrangements`为上游README中的旧写法；远端实际论文分支经核验为`mesh-arrangements`，后续使用后者，不据旧拼写执行。

1. 新生成的普通研究文档使用中文文件名，并以 `01-、02-、...` 顺序编号；创建前扫描同目录现有编号并顺延。综合规划与专题调研文档统一放入 `课题规划与专题调研/`，下一建议编号为 `26-`；`文献精读/` 下一建议编号为 `13-`。根目录不再直接堆放普通研究文档。`AGENTS.md`、`README.md`、许可证和构建配置等约定文件不参与编号。
2. 每份新文档开头必须包含：
   - 生成时间（年月日时分秒，北京时间）；
   - 修改时间及修改内容；
   - 文档概述；
   - 索引目录。
3. 引用具体结果时注明来源层级：全文、摘要、会议摘要、预印本、第三方实现或厂商资料。
4. 厂商资料和厂商雇员研究必须标注宣传属性或利益冲突；会议摘要不得描述为完整同行评审论文。
5. 外部链接、期刊等级、源码状态和产品能力可能变化。需要写“当前仍可用/当前最新”时必须重新联网核验，并记录核验日期。
6. 不编造源码地址、实验数值、样本量、临床结论或“文献空白”。未找到证据时明确写“未检索到”或“待核对”。
7. 图表应保存生成脚本、输入数据和参数，保证可复现；不要只保留截图。
8. **研究内容范本维护**：开展研究规划、方法设计、实验方案、阶段汇报或论文撰写前，先读取根目录 `研究内容.md`，明确本次工作与三项研究的对应关系。用户或导师调整方向，或新实验实质性改变方法可行性、适用范围、创新定位及完成状态时，应在同次任务中同步更新范本及直接受影响的规划表述；日常运行记录与个别参数变化写入实验报告，不机械扩充范本。更新须有可追溯证据，区分已有基础、拟研究方法与已验证结论，不将失败改写为成功，不静默扩大研究范围。
9. **指定格式例外**：依用户2026-09-07的明确要求，`研究内容.md` 固定放在项目根目录，使用中文文件名且不加数字前缀，主体保留三个研究内容及其学术短标题；依同日后续要求，允许追加“与原始课题内容的对应关系”附录，包含对应表、进一步凝练之处和研究主次定位。该附录不构成第四项研究，不增设前言、元数据、索引或进度汇总。该文件的生成时间、后续修改时间（北京时间）、修改内容及主要依据统一记录在本 `AGENTS.md` 的修改记录中；首次生成依据为06号汇报、04号实施方案、07号AMBER方案以及09、11、12号原型与实验报告。其他文档仍遵守原编号和元数据规范。

## 九、开始任务前的检查清单

开始任何非琐碎任务前，依次确认：

1. 已否读取 `研究内容.md`？当前任务属于其中哪项研究，还是文献/环境辅助任务？
2. 是否遵循18号v2实验规划？本次研究问题、数学依据、已有算法基线、消融及验收条件是否明确？GPU必做环节和CPU参照如何对应？
3. 是否超出“肩胛盂局部、刚性骨、球形磨钻、非临床验证”的范围？
4. 是否有可先运行的现有实现、最小示例或基线？
5. 成功标准能否用几何、性能、质量或稳定性指标验证？
6. 是否会修改第三方参考仓库、原始数据或初始 DOCX？如会，先停止并核对授权与备份策略。
7. 若要新增文档，是否已计算同目录下一编号并写入完整北京时间元数据？

在上述问题没有明确答案时，先补充证据或向用户说明关键歧义；不要静默扩大任务范围。

## 十、当前运行环境

**2026-09-08 14:27界面与RXMesh**：本机现有`.venv`运行`初步实验/真实骨模型演示/research_viewer.py`查看26/27号网格和曲线；原入口右侧新增“研究结果与指标曲线”和“显示 / 隐藏在线指标曲线”。测试为`test_research_results.py`、`research_gui_smoke.py`、`live_metrics_smoke.py`。研究窗口是证据回放，原窗口曲线消费实际已完成记录，两者不得混淆；缺失质量/误差不填零。RXMesh隔离路径`/root/autodl-tmp/graduation_project/build_rxmesh_dynamic_20260908_141858`仅上传源码并失败配置；`run_build.sh`计划创建cmake_venv安装cmake3.31.6但尚未执行，SSH不可达。恢复前核对用户实例状态及固定主机指纹，详见28号及RXMesh动态对照目录。

**2026-09-08 13:00区间优化**：沿用CUDA远端stage1和既有venv，无新增依赖。`CUDA真实骨面对照/interval_sweep.cu`用double定向舍入构造区间，`IntervalPatch`正式轮不做CPU镜像，而将整个区间宽度计入证书；宽度>1e-10 mm或输入超域即拒绝，无静默回退。验证轮额外全查询CPU对拍，与正式计时分开。不得把普通CPU重算称为精确回退，也不得将扫掠区间包含等同于原面定位及整系统证明。`benchmark_interval.py`复现3组3轮16步及独立验证，正式证据为`区间实验结果/20260908_125526`；首轮125113保留。`benchmark_incremental.py`的缓存/驻留5组为负例，不接默认GUI。`prepare_bundle.py --optimization`生成独立39文件白名单快照及摘要，不覆盖24号归档，不含.env。共享PatchModel查询批次默认64不变，512只用于显式消融；不减少格点或修改门槛。下一步仍须完整时序、强基线后维护和原138步；实例未关闭。图及统计使用显式路径，详见25号。

**2026-09-08 12:29真实CUDA管线**：本轮远端stage1位于`/root/autodl-tmp/graduation_project/stage1`。新增`初步实验/CUDA真实骨面对照/`，prepare_bundle.py白名单打包代码和公开STL/初态NPZ，严禁打包.env。requirements.txt在既有远端venv安装与本机相同几何依赖，并增加cupy-cuda12x13.6.0、fastrlock0.8.3；原系统Python包不卸载。CUDA RawKernel double、关闭FMA、无fast-math，仍保留CheckedGpuPatch逐查询CPU审计，原OpenCL模块改为按需导入。run_remote.sh设北京时间与4线程预算并串行执行35项检查及实验；Geogram仅请求sys:max_threads=4，底层TBB并行实际线程尚未独立审计，不把该参数当作硬件占用证明。初始Geogram boolean_op的OBJ六位有效数字不适合连续回灌，不能据首轮差面宣称算法劣势；独立geogram_double_io.cpp调用原mesh_difference，仅更换17位有效数字I/O，已通过原整骨往返及解析差集检查。不要修改上游mesh_io.cpp或放宽质量门槛。正式证据为121340的CPU/CUDA测量与122455的Geogram重跑，后者复用前者CPU数据，不算额外重复。结果、图和限制见24号报告，尚未完成GPU免CPU审计数值保障、Geogram后质量维护、GUI或原138步。

**2026-09-08 11:59远程CUDA已接入**：用户授权设备实测RTX 4080 SUPER，驱动580.105.08、Toolkit12.8.93、计算能力8.9；云端上报32760 MiB显存，不据此推断零售硬件规格。远端根目录`/root/autodl-tmp/graduation_project`，使用预装`/root/miniconda3/bin/python`创建`venv --system-site-packages`，Python3.12.3、PyTorch2.8.0+cu128、NumPy2.3.2。未重装驱动/Toolkit，SSH非交互PATH须显式设置。CPU为Xeon8260，配额12核，首批使用4线程。本机用`uv run --no-project --with paramiko==5.0.0 python -X utf8 初步实验/CUDA远程验证/remote.py --command 'nvidia-smi'`连接；主机指纹TOFU存入`.ssh_known_hosts`，密钥变化拒绝；初次指纹未独立确认。禁止上传`.env`、将密码写入报告/日志或提交Git。`run_stage0.sh`按北京时间生成新结果，`run_geogram.sh`无窗口Release构建当前固定Geogram快照；系统已有TBB需`-DCMAKE_CXX_STANDARD_LIBRARIES=-ltbb`，首轮缺失链接失败日志保留。CUDA编译`sm_89`、double、关闭FMA/fast-math，全部原始结果见23号。未配置RXMesh/PaMO环境，未接入真实骨面GUI，实例保持运行，不能擅自释放租赁资源。

**2026-09-08路线更新**：本机不安装CUDA不等于课题不采用CUDA。当前CPU与OpenCL结果保留，未来必须在CUDA设备开展正式计算与端到端验证。远程设备、驱动、Toolkit、计算能力及编译参数届时实测记录；Docker不能让Intel核显执行CUDA。本次仅将代码拉至`reference/近期强基线_20260908/`，未构建、安装运行依赖或改动旧参考仓库。PDF检查借用Codex捆绑Python中的pypdf与pypdfium2，未修改项目`.venv`。当前快照不等于论文发表时精确版本，正式复现实验前仍须核对。

1. 当前正式环境位于项目根目录 `.venv`，Python 版本为 3.13；现有实验和交互式桌面原型均使用该环境，窗口层采用 PyVistaQt、VTK 与 PyQt6，避免同时维护重复的 Conda 环境。
2. 环境没有内置 `pip`，统一用 `uv` 安装依赖：
   - 几何实验：`uv pip install --python .\.venv\Scripts\python.exe -r .\初步实验\E5lite_局部化增量\requirements.txt`
   - 交互原型：`uv pip install --python .\.venv\Scripts\python.exe -r .\初步实验\真实骨模型演示\requirements_interactive.txt`
3. 交互程序入口：`.\.venv\Scripts\python.exe .\初步实验\真实骨模型演示\real_bone_interactive_app.py`。
4. 本机可使用 Conda 和 Docker。若 `.venv` 无法恢复，可用 Conda 创建 Python 3.13 环境并安装同一依赖清单；Docker 适合无窗口批量实验与可复现实验，不作为 Windows 桌面交互演示的默认入口。
5. 质量交付增加`pymeshlab==2025.7.post1`，已写入交互依赖清单，仍使用同一`.venv`。入口目录中的`test_quality_delivery.py`运行解析测试，`quality_experiment.py`复现实验，`quality_report.py`生成10号报告及图，实验结果存入`网格质量交付实验/`。
6. 原入口的优化仍是暂停检查点分析副本，不能宣称每帧实时重网格。验收包含水密、绕序、欧拉示性数、退化、自相交、抽样表面距离和体积变化。继续磨削使旧副本失效；原入口计量结果保持取自原始计算状态。必须报告新增计划计量后的实测耗时，不沿用旧管线性能数字。
7. 新增独立逐步实验入口：`.\.venv\Scripts\python.exe .\初步实验\真实骨模型演示\dynamic_quality_app.py`。每次维护后通过门控才发布并回灌，失败即回滚并停止；当前不是实时达标版本，也不是逐三角形最小角保证。全骨差面<1%、工具邻域差面<5%只是实验占比门槛，不代表下游求解器认可。
8. `dynamic_quality.py`运行批量实验，`test_dynamic_quality.py`验证回滚/回灌/双精度，`dynamic_gui_smoke.py`验证三步窗口，`dynamic_quality_report.py`生成11号报告。结果位于`逐步质量维护实验/`，保留时间戳run记录、最后合格网格及失败候选。动态NPZ保留双精度；PLY仅作预览，不能套用NPZ的验收结论。
9. 本次沿用同一`.venv`及现有依赖，未另建Conda或Docker。探索时额外安装过`plyfile==1.1.5`，最终实现未使用，不属于运行必需依赖。GUI退出存在nanobind引用泄漏警告，尚需长期内存稳定性检查。
10. 第一阶段解析实验位于`初步实验/局部区域重建阶段一/`，沿用同一环境，无新增依赖。依次运行`test_patch.py`、`experiment.py`、`long_sequence.py`、`report.py`复现六项测试、27组实验、64步重复序列及12号报告。该原型仅支持无倒扣单值高度图、水平等高球心扫掠和固定外边界；h=0.25/0.16 mm的六类常规序列通过逐面q≥0.4、角≥25°及垂直误差上界≤0.1 mm门槛。不得把这一受限机制替代真实骨面全序列、动态空腔重三角化或下游求解器验证。数据和代码摘要位于其`实验结果/`。
11. 真实骨面图域实验位于`初步实验/真实骨面高度图验证/`，依次运行`test_projection.py`、`experiment.py`、`report.py`，并回归上一阶段`test_patch.py`。沿用同一`.venv`，无新依赖/Conda/Docker。输出为开放局部面及NPZ/JSON证据；源面须通过覆盖、无重叠、无遮挡和内部边界检查后才能作为图域。原12.5 mm计划圆盘存在骨轮廓外未命中区，不可补成骨面；原深台阶与垂直柱钻尚不受支持。共享解析求解器增加高度查询覆盖点和可选区域半宽，默认8 mm解析行为不变。
12. 共边拼接实验位于`初步实验/真实骨面共边拼接/`，同一环境新增`triangle==20250106`，安装命令：`uv pip install --python .\.venv\Scripts\python.exe -r .\初步实验\真实骨面共边拼接\requirements.txt`。依次运行该目录`test_stitch.py`、`experiment.py`、`report.py`，回归图域7项与解析6项测试。Triangle固定原边界线段，新面共享全局索引，外部原面不变；三维逐面门槛与自交检查统一执行。12组均未完成初始联合验收，不能作为实时磨削入口。正式结果是带g0/g1后缀的12个候选及results.json，无g后缀的6个文件为首轮探索证据。原始顶点索引保留可能产生未引用顶点，拓扑欧拉数按引用顶点计。依赖底层Triangle不是无条件可商用许可，详见17号报告；没有创建Conda/Docker。
13. 联合重建新入口位于`初步实验/边界过渡带联合重建/`，沿用现有`.venv`，没有新增依赖。依次运行`test_joint.py`、`hardware_probe.py`、`experiment.py`、`audit_saved.py`、`report.py`；`profile_run.py`单独复测显式包含式计时（各项不能直接相加）。`replay.py`是合格整骨序列的离线回放，可拖动滑块、左右方向键单步、鼠标旋转缩放；`--screenshot`输出截图。不是在线实时计算，未替换原GUI入口。旧stitch_candidate仅增加默认不启用的extra_points参数。新外边界共享索引，质量反馈扩域、误差反馈补点；所有新面与过渡带一起验收。深切/原138步尚不支持，禁止宣称通用质量保证。
14. 本机没有独显，实测设备名为`Intel(R) Arc(TM) 130T GPU (16GB)`，驱动`32.0.101.6554`。通过已有`OpenCL.dll`枚举得到OpenCL 3.0 NEO及`cl_khr_fp64`声明；名称中的16GB不是独立显存容量证据。20号已实际运行双精度扫掠核并与CPU对拍，不代表已验证原生双精度硬件吞吐或整条管线加速；不安装/默认使用CUDA。GPU必做要求不取消；若需外部GPU资源须另行确认，不自行租用。
15. 19号采用完整报警面集合的精确投影分离复核，保留PyMeshLab原始报警；不能证明严格分离的接触、重叠、单面孤立报警仍拒绝，不是关闭自交检测。数值边界容差为1e-10 mm并计入误差预算；验证格点可加密到512，不改变网格与0.1 mm门槛。旧上限256的失败保存在探索JSON。探索性cProfile出现不一致，不用于性能结论；正式使用显式perf_counter记录。未修改原骨面、旧实验结论或第三方参考源码。
16. 20号入口`初步实验/局部适用域与核显计算/`：同一`.venv`新增`pyopencl==2026.1.4`、传递依赖`pytools==2026.1.1`和`siphash24==1.8`。安装：`uv pip install --python .\.venv\Scripts\python.exe -r .\初步实验\局部适用域与核显计算\requirements.txt`。没有配置Conda/Docker或额外驱动。依次运行`experiment.py`、`gpu_experiment.py`、`audit.py`、`boundary_check.py`、`report.py`；GPU计时不得与几何实验并行。`sweep.cl`使用double与CL1.2，不启用fast-math，不支持GPU不可用时静默退回CPU。20号仅算子微基准，21号已接入实际查询及证书但未接GUI。局部判据沿用逐面门槛与整骨复核，不是最终暂停优化，亦非通用动态扩域。
17. 21号在同目录新增`integrated.py`、`integrated_experiment.py`、`boolean_comparison.py`、`boolean_audit.py`、`integrated_report.py`和`test_integrated.py`。沿用依赖，无环境变更。每轮输出北京时间新目录；审计/报告默认最新目录，须确认结果完整。CheckedGpuPatch逐查询CPU审计并补入实测差值，费用计入总耗时，不得删除审计后沿用其验收结论。Mesh64连续布尔参照未做完整证书/自交或后重网格，质量失败后的诊断续跑不计合格发布，不能和完整门控管线直接排名速度。31项测试、96次CPU/GPU接受请求与32次布尔诊断保留；原138步仍未完成。
